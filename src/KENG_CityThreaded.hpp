#include "KENG_City.hpp"

#ifdef __KENG_CITY_HPP__

#include <thread> /* Ughh... */
#include <mutex>
#include <atomic>

/* TODO: Make rendering cities concurrent to the main thread, although the SDL2 rendering functions
are not thread safe.... (ughhh.....) */

#endif
